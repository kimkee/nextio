import type { Metadata } from "next";
// import localFont from "next/font/local";
import "./globals.scss";
import { SpeedInsights } from '@vercel/speed-insights/next';
import { GoogleAnalytics } from '@next/third-parties/google';
import { Inter, Roboto_Mono, Noto_Sans } from 'next/font/google';
import { usePathname } from "next/navigation";
export const metadata: Metadata = { title: "NEXTIO", description: "Generated by create next app" };
export const viewport = { width: 'device-width', initialScale: 1, maximumScale: 1, minimumScale: 1, userScalable: false, viewportFit: 'cover', shrinkToFit: false }

const noto_sans = Noto_Sans({ subsets: ['latin'], display: 'swap', });

import Header from '@/app/components/Header';
import Nav from '@/app/components/Nav';

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {

  return (

    <html lang="ko">
      <body className={`body ${noto_sans.className} antialiased`}>
        <div className="wrap">
          <Header />
          {children}
          <Nav />
        </div>
        {process.env.NEXT_PUBLIC_SITE_URL === 'https://nextio.vercel.app/' && <SpeedInsights />}
        <GoogleAnalytics gaId="G-9BBCJ4KC3Z" />{/* Cloudflare Web Analytics */}
      </body>
    </html>
  );
}
